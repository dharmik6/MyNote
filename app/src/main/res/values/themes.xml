<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyNote" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorAccent">@color/base_green</item>
    </style>

    <style name="Theme.MyNote" parent="Base.Theme.MyNote" />

    <style name="Theme.Toolbar.NavigationView" parent="ThemeOverlay.Material3.NavigationView" >
        <item name="android:listDivider">@color/base_grey</item>
        <item name="android:textColorSecondary">@color/white</item>
    </style>
</resources>